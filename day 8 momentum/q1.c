// chek number is even or odd using ternary //

#include <stdio.h>
int main()
{
    int num;
    printf("enetr thr number");
    scanf("%d",&num);

    num%2==0 ? printf("The number is even") : printf("The number is odd");
}